function setup() {
    $("body").append(
        "<h1>Welcome to Ogre Fighter v1.0</h1>" +
        "<code>Press CTRL + SHIFT + ESC</code>"
    );
}

// Starts when HTML document ready.
$("document").ready(setup);